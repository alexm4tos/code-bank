import Head from 'next/head';
import { NextPage } from 'next/types';

const Home: NextPage = () => {
	return (
		<>
			<Head>
				<title>Store frontend</title>
			</Head>

			<h1>Store frontend</h1>
		</>
	);
};

export default Home;
